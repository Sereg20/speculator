/**
 * Standard API response envelope.
 * Every route returns this shape.
 *
 * @typedef {Object} ApiResponse
 * @property {*|null} data
 * @property {string|null} error
 * @property {{ ingameDay: number, energyCurrent: number, energyMax: number }|null} meta
 */

export {}; // types only — no runtime code
