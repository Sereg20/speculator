/** Phase 5 — resolves a buy or sell negotiation attempt. */
export async function resolveNegotiation(_playerId, _carId, _offerDelta, _context) {
  throw new Error('negotiationEngine: Not implemented (Phase 5)');
}
